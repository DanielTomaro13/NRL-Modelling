# Proprietary bookmaker pricing engine — reference only, NEVER commit
MonteCarlo/

# macOS
.DS_Store
**/.DS_Store

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Local odds cache / scratch
*.tmp
reports/_synth_*

# Local editor / preview tooling
.claude/

# Secrets / bookmaker tokens — NEVER commit (use env vars or GitHub secrets)
.env
.env.*
!.env.example
*.token
*secret*
tokens.json
dabble_token*
tab_token*
